// Saved manga logic (for future Firebase integration)
document.addEventListener('DOMContentLoaded', () => {
    const savedList = document.querySelector('.saved-list');
    savedList.innerHTML = `<h2>Saved manga will appear here (feature coming soon)</h2>`;
});
